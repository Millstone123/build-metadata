# build-metadata

Build system metadata validator. Ensures project build configuration is internally consistent across environments.

## Setup

```bash
pip install -r requirements.txt
pip install -e .
python3 -m pytest test_build.py
```
